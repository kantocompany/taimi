name: Daily Update

on:
  schedule:
    - cron: "0 5 * * *" # 05:00 UTC (07:00/08:00 Helsinki)
  workflow_dispatch:

concurrency:
  group: taimi-update
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v6

      - name: Check for existing update PR
        id: check-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          open_prs=$(gh pr list --label automated-update --state open --json number --jq 'length')
          if [ "$open_prs" -gt 0 ]; then
            echo "::notice::Open automated-update PR exists. Skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set date
        if: ${{ !steps.check-pr.outputs.skip }}
        run: echo "DATE=$(date -u +%Y-%m-%d)" >> "$GITHUB_ENV"

      - name: Create update branch
        if: ${{ !steps.check-pr.outputs.skip }}
        run: |
          git checkout -B "automated-update/${{ env.DATE }}"
          echo "BRANCH=automated-update/${{ env.DATE }}" >> "$GITHUB_ENV"

      - name: Run Claude Code agent
        if: ${{ !steps.check-pr.outputs.skip }}
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          show_full_output: true
          prompt: |
            Today is ${{ env.DATE }}. Read docs/update-runbook.md and execute a full update cycle. Do not commit or push — the workflow handles git operations.
          claude_args: |
            --model claude-sonnet-4-6
            --max-turns 30
            --max-budget-usd 5
            --allowedTools "Read,Edit,Write,Glob,Grep,WebSearch,WebFetch,Bash(./scripts/*),Bash(jq *),Bash(git diff *),Bash(git status),Bash(git log *)"
            --disallowedTools "Bash(rm *),Bash(curl *),Bash(wget *),Bash(npm *),Bash(pip *),Bash(sudo *)"
            --verbose

      - name: Post-agent validation
        if: ${{ !steps.check-pr.outputs.skip }}
        run: |
          ./scripts/generate-tool-files.sh
          ./scripts/validate.sh

      - name: Check for changes
        if: ${{ !steps.check-pr.outputs.skip }}
        id: changes
        run: |
          git add public/
          if git diff --cached --quiet; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No changes in public/. Skipping PR."
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: ${{ !steps.check-pr.outputs.skip && steps.changes.outputs.has_changes == 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: automated pricing update ${{ env.DATE }}"
          git push -u origin "$BRANCH"

      - name: Create pull request
        if: ${{ !steps.check-pr.outputs.skip && steps.changes.outputs.has_changes == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --title "Automated pricing update ${{ env.DATE }}" \
            --label "automated-update" \
            --body "## Automated Update

          Created by the daily update workflow running Claude Code agent against the [update runbook](docs/update-runbook.md).

          ### Review checklist
          - [ ] Only \`public/\` files changed
          - [ ] \`changelog.json\` has entries with source URLs
          - [ ] Price changes verified against vendor pricing pages
          - [ ] \`validate.sh\` passed (check Actions log)
          - [ ] Observations in index.html still factually correct
          - [ ] No UNVERIFIED markers left unaddressed

          ### Cost
          Check the Claude Code step log for actual token usage.

          ---
          Generated by [Claude Code](https://claude.com/claude-code) via GitHub Actions"
